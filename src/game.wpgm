import wollok.game.*
import config.*
import entities.*
import attack.*

program game {
	
	var enemy = new Enemy()
	
	game.cellSize(20)
	
	game.height(42)
	game.width(72)
	
	game.title("Nightmare in Nordelta")
	
	background.level(2)
//	level1.background()
	level1.setupEntity(enemy, game.at(58.5,9), 40, "DynamicPose", 24)
	level1.setupEntity(capybaraPlayer, game.at(0,8), 40, "DynamicPose", 24)
	general.keyAssigments()
	general.movement(capybaraPlayer)
//	general.movement(enemy)
	
	game.start()
}
